<mah:MetroWindow
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    x:Class="FlowBlox.UICore.PopUp.Views.QuickStartWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resources="clr-namespace:FlowBlox.UICore.PopUp.Resources"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    mc:Ignorable="d"
    Width="560"
    Height="760"
    ResizeMode="NoResize"
    ShowMinButton="False"
    ShowMaxRestoreButton="False"
    WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FlowBlox.UICore;component/Views/MahAppMetroResources.xaml" />
                <ResourceDictionary Source="/FlowBlox.UICore;component/Views/Styles/FlowBloxCustomStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeadlineTextBlock"
                   Grid.Row="0"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="#1F2937"
                   TextWrapping="Wrap"
                   Margin="0,0,0,14" />

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    BorderBrush="#D8E0EA"
                    BorderThickness="1"
                    Background="#F8FAFC"
                    CornerRadius="4"
                    ClipToBounds="True">
                <Grid>
                    <Image x:Name="StepImage"
                           Stretch="Uniform"
                           SnapsToDevicePixels="True" />
                    <TextBlock x:Name="ImagePlaceholderTextBlock"
                               Text="{x:Static resources:SequenceDetectionPopUpTexts.Image_Placeholder}"
                               Foreground="#6B7280"
                               FontSize="14"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Visibility="Collapsed" />
                </Grid>
            </Border>

            <ItemsControl x:Name="StepIndicator"
                          Grid.Row="1"
                          HorizontalAlignment="Center"
                          Margin="0,14,0,12">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse Width="9"
                                 Height="9"
                                 Margin="5,0"
                                 Fill="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock x:Name="DescriptionTextBlock"
                       Grid.Row="2"
                       Foreground="#374151"
                       FontSize="14"
                       TextWrapping="Wrap"
                       LineHeight="20" />
        </Grid>

        <DockPanel Grid.Row="2"
                   Margin="0,18,0,0"
                   LastChildFill="False">
            <CheckBox x:Name="ShowAgainCheckBox"
                      DockPanel.Dock="Left"
                      VerticalAlignment="Center"
                      Content="{x:Static resources:SequenceDetectionPopUpTexts.CheckBox_ShowAgain}" />

            <Button x:Name="NextButton"
                    DockPanel.Dock="Right"
                    MinWidth="120"
                    IsDefault="True"
                    Click="NextButton_Click">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial x:Name="NextButtonIcon"
                                                Kind="ArrowRight"
                                                Style="{StaticResource FlowBlox.WindowActionIcon.Save}" />
                    <TextBlock x:Name="NextButtonTextBlock" />
                </StackPanel>
            </Button>
        </DockPanel>
    </Grid>
</mah:MetroWindow>
