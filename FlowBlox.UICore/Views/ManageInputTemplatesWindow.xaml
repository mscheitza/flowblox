<mah:MetroWindow
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    x:Class="FlowBlox.UICore.Views.ManageInputTemplatesWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:FlowBlox.UICore.ViewModels"
    xmlns:converters="clr-namespace:FlowBlox.UICore.Converters"
    xmlns:resources="clr-namespace:FlowBlox.UICore.Resources"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    Title="{x:Static resources:ManageInputTemplatesWindow.Title}"
    Height="760"
    Width="1100"
    mc:Ignorable="d"
    WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FlowBlox.UICore;component/Views/MahAppMetroResources.xaml" />
                <ResourceDictionary Source="/FlowBlox.UICore;component/Views/Styles/FlowBloxCustomStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
            <converters:EnumDisplayNameConverter x:Key="EnumDisplayNameConverter"/>

            <Style x:Key="ManageInputTemplates.Icon.Open"
                   TargetType="{x:Type iconPacks:PackIconMaterial}"
                   BasedOn="{StaticResource FlowBlox.ToolbarIconBase}">
                <Setter Property="Foreground" Value="#2F6DB3"/>
            </Style>

            <Style x:Key="ManageInputTemplates.Icon.Create"
                   TargetType="{x:Type iconPacks:PackIconMaterial}"
                   BasedOn="{StaticResource FlowBlox.ToolbarIconBase}">
                <Setter Property="Foreground" Value="#2E8B57"/>
            </Style>

            <Style x:Key="ManageInputTemplates.Icon.Upload"
                   TargetType="{x:Type iconPacks:PackIconMaterial}"
                   BasedOn="{StaticResource FlowBlox.ToolbarIconBase}">
                <Setter Property="Foreground" Value="#1F8A8A"/>
            </Style>

            <Style x:Key="ManageInputTemplates.Icon.Download"
                   TargetType="{x:Type iconPacks:PackIconMaterial}"
                   BasedOn="{StaticResource FlowBlox.ToolbarIconBase}">
                <Setter Property="Foreground" Value="#3A6EA5"/>
            </Style>

            <Style x:Key="ManageInputTemplates.Icon.Locate"
                   TargetType="{x:Type iconPacks:PackIconMaterial}"
                   BasedOn="{StaticResource FlowBlox.ToolbarIconBase}">
                <Setter Property="Foreground" Value="#5C6B73"/>
            </Style>

            <Style x:Key="ManageInputTemplates.Icon.Delete"
                   TargetType="{x:Type iconPacks:PackIconMaterial}"
                   BasedOn="{StaticResource FlowBlox.ToolbarIconBase}">
                <Setter Property="Foreground" Value="#C94F4F"/>
            </Style>
            
        </ResourceDictionary>
    </Window.Resources>

    <DockPanel Margin="10">

        <!-- Header -->
        <Border Background="#FFEEEEEE"
                DockPanel.Dock="Top"
                Height="50"
                Padding="8">
            <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconMaterial Kind="FileDocumentMultipleOutline"
                                            Width="30"
                                            Height="30"
                                            Margin="0,0,10,0"
                                            VerticalAlignment="Center"
                                            Foreground="DarkSlateGray"/>
                <TextBlock Text="{x:Static resources:ManageInputTemplatesWindow.Header_Title_Main}"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           FontSize="16"/>
            </StackPanel>
        </Border>

        <!-- Description -->
        <Border Background="#EDF3F9"
                DockPanel.Dock="Top"
                Padding="12"
                CornerRadius="4"
                Margin="0,8,0,10">
            <StackPanel>
                <TextBlock Text="{x:Static resources:ManageInputTemplatesWindow.Header_Description_Main}"
                           FontWeight="UltraLight"
                           Foreground="DarkSlateGray"
                           TextWrapping="Wrap"/>
                <TextBlock Margin="0,8,0,0"
                           Text="{Binding ProjectInputDirectory}"
                           FontWeight="SemiBold"
                           Foreground="#3A6EA5"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Toolbar -->
            <ToolBar Grid.Row="0" VerticalAlignment="Center">

                <Button Command="{Binding OpenInputDirectoryCommand}"
                        ToolTip="{x:Static resources:ManageInputTemplatesWindow.Tooltip_OpenInputDirectory}">
                    <iconPacks:PackIconMaterial Kind="FolderOpenOutline"
                                    Style="{StaticResource ManageInputTemplates.Icon.Open}"/>
                </Button>

                <Separator/>

                <Button Command="{Binding CreateTemplateCommand}"
                        ToolTip="{x:Static resources:ManageInputTemplatesWindow.Tooltip_CreateTemplate}">
                    <iconPacks:PackIconMaterial Kind="FilePlusOutline"
                                    Style="{StaticResource ManageInputTemplates.Icon.Create}"/>
                </Button>

                <Button Command="{Binding UploadTemplateCommand}"
                        IsEnabled="{Binding IsTemplateSelected}"
                        Margin="6,0,0,0"
                        ToolTip="{x:Static resources:ManageInputTemplatesWindow.Tooltip_UploadTemplate}">
                    <iconPacks:PackIconMaterial Kind="UploadOutline"
                                    Style="{StaticResource ManageInputTemplates.Icon.Upload}"/>
                </Button>

                <Button Command="{Binding DownloadTemplateCommand}"
                        IsEnabled="{Binding IsTemplateSelected}"
                        Margin="6,0,0,0"
                        ToolTip="{x:Static resources:ManageInputTemplatesWindow.Tooltip_DownloadTemplate}">
                    <iconPacks:PackIconMaterial Kind="DownloadOutline"
                                    Style="{StaticResource ManageInputTemplates.Icon.Download}"/>
                </Button>

                <Button Command="{Binding OpenTemplateInExplorerCommand}"
                        IsEnabled="{Binding IsTemplateSelected}"
                        Margin="6,0,0,0"
                        ToolTip="{x:Static resources:ManageInputTemplatesWindow.Tooltip_OpenTemplateInExplorer}">
                    <iconPacks:PackIconMaterial Kind="FolderSearchOutline"
                                    Style="{StaticResource ManageInputTemplates.Icon.Locate}"/>
                </Button>

                <Button Command="{Binding RemoveTemplateCommand}"
                        IsEnabled="{Binding IsTemplateSelected}"
                        Margin="10,0,0,0"
                        ToolTip="{x:Static resources:ManageInputTemplatesWindow.Tooltip_RemoveTemplate}">
                    <iconPacks:PackIconMaterial Kind="DeleteOutline"
                                    Style="{StaticResource ManageInputTemplates.Icon.Delete}"/>
                </Button>

            </ToolBar>

            <!-- Grid -->
            <DataGrid Grid.Row="1"
                      ItemsSource="{Binding Templates}"
                      SelectedItem="{Binding SelectedTemplate}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="False"
                      Margin="0,10,0,0">
                <DataGrid.Columns>

                    <!-- Read-only "InputDir" column to make the base directory explicit -->
                    <DataGridTextColumn Header="{x:Static resources:ManageInputTemplatesWindow.Header_InputDir}"
                                        Binding="{Binding DataContext.ProjectInputDirectory, RelativeSource={RelativeSource AncestorType=Window}}"
                                        IsReadOnly="True"
                                        Width="*"/>

                    <DataGridTextColumn Header="{x:Static resources:ManageInputTemplatesWindow.Header_RelativePath}"
                                        Binding="{Binding RelativePath, UpdateSourceTrigger=PropertyChanged}"
                                        Width="2*"/>

                    <DataGridTextColumn Header="{x:Static resources:ManageInputTemplatesWindow.Header_FileName}"
                                        Binding="{Binding FileName}"
                                        IsReadOnly="True"
                                        Width="*"/>

                    <DataGridTextColumn Header="{x:Static resources:ManageInputTemplatesWindow.Header_Size}"
                                        Binding="{Binding SizeBytes}"
                                        IsReadOnly="True"
                                        Width="120"/>

                    <DataGridTemplateColumn Header="{x:Static resources:ManageInputTemplatesWindow.Header_SyncMode}"
                                            Width="190">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SyncMode, Converter={StaticResource EnumDisplayNameConverter}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding DataContext.SyncModes, RelativeSource={RelativeSource AncestorType=Window}}"
                                          SelectedItem="{Binding SyncMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Converter={StaticResource EnumDisplayNameConverter}}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>

            <!-- Bottom buttons -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,10,0,0">
                <Button Command="{Binding CloseCommand}" IsCancel="True">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Close" Margin="0,0,5,0"/>
                            <TextBlock Text="{x:Static resources:ManageInputTemplatesWindow.Button_Close}"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </StackPanel>

        </Grid>
    </DockPanel>
</mah:MetroWindow>
