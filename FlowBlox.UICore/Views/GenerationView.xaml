<mah:MetroWindow  
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    x:Class="FlowBlox.UICore.Views.GenerationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:FlowBlox.UICore.ViewModels"
    xmlns:converters="clr-namespace:FlowBlox.UICore.Converters.TestDefinition"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:resources="clr-namespace:FlowBlox.UICore.Resources"
    Title="{x:Static resources:GenerationView.Title}" 
    Height="600" Width="800"
    mc:Ignorable="d">

    <Window.DataContext>
        <viewModel:GenerationViewModel />
    </Window.DataContext>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FlowBlox.UICore;component/Views/MahAppMetroResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <converters:LogLevelToIconConverter x:Key="LogLevelToIconConverter"/>
            <viewModel:GenerationViewModel x:Key="GenerationViewModel"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListView Grid.Row="0" ItemsSource="{Binding RuntimeLogs}">
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="{x:Static resources:GenerationView.ContextMenu_OpenInEditor}" 
                              Command="{Binding OpenInEditorCommand, Source={StaticResource GenerationViewModel}}" 
                              CommandParameter="{Binding PlacementTarget.SelectedItem, RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}}">
                        <MenuItem.Icon>
                            <iconPacks:PackIconModern Kind="Edit" Width="14" Height="14" Margin="0,0,5,0"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </ContextMenu>
            </ListView.ContextMenu>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="{x:Static resources:GenerationView.Column_Timestamp}" DisplayMemberBinding="{Binding Timestamp}"/>
                    <GridViewColumn Header="{x:Static resources:GenerationView.Column_LogLevel}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <iconPacks:PackIconModern Kind="{Binding LogLevel, Converter={StaticResource LogLevelToIconConverter}}" Width="16" Height="16" Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding LogLevel}"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="{x:Static resources:GenerationView.Column_Message}" DisplayMemberBinding="{Binding Message}"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="{x:Static resources:GenerationView.Button_Close}" IsCancel="True" Click="CloseButton_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</mah:MetroWindow>
