<UserControl x:Class="FlowBlox.UICore.Views.ComponentLibraryViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:resources="clr-namespace:FlowBlox.UICore.Resources"
             xmlns:viewModels="clr-namespace:FlowBlox.UICore.ViewModels.ComponentLibrary"
             mc:Ignorable="d"
             d:DesignHeight="520"
             d:DesignWidth="320">

    <UserControl.DataContext>
        <viewModels:ComponentLibraryViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FlowBlox.UICore;component/Views/Styles/FlowBloxCustomStyles.xaml" />
                <ResourceDictionary Source="/FlowBlox.UICore;component/Views/Styles/HostedControlStyles.xaml" />
                <ResourceDictionary Source="/FlowBlox.UICore;component/Views/Styles/TreeViewStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="ComponentLibrary.ToolbarButtonStyle"
                   TargetType="{x:Type Button}">
                <Setter Property="Height" Value="28" />
                <Setter Property="Margin" Value="0,0,6,0" />
                <Setter Property="Padding" Value="6,0" />
                <Setter Property="Background" Value="#FFF3F6FA" />
                <Setter Property="BorderBrush" Value="#FFD1D9E2" />
                <Setter Property="BorderThickness" Value="1" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#FFE9EEF4" />
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="#FFDDE6F0" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ToolBarTray Grid.Row="0"
                     Background="Transparent"
                     IsLocked="True">
            <ToolBar Background="Transparent"
                     BorderThickness="0"
                     Padding="0"
                     Margin="0">
                <Button Command="{Binding ManageExtensionsCommand}"
                        Style="{StaticResource ComponentLibrary.ToolbarButtonStyle}"
                        ToolTip="{x:Static resources:ComponentLibraryView.Toolbar_ManageExtensions_Tooltip}">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="PuzzleOutline"
                                                    Width="16"
                                                    Height="16"
                                                    Margin="0,0,5,0"
                                                    VerticalAlignment="Center"
                                                    Foreground="#2E7D32" />
                        <TextBlock Text="{x:Static resources:ComponentLibraryView.Toolbar_ManageExtensions}"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </ToolBar>
        </ToolBarTray>

        <DockPanel Grid.Row="1"
                   LastChildFill="True"
                   Margin="0,6,0,8">
            <iconPacks:PackIconMaterial Kind="Magnify"
                                        Width="16"
                                        Height="16"
                                        Margin="0,0,6,0"
                                        VerticalAlignment="Center" />
            <TextBox Height="30"
                     MinHeight="30"
                     VerticalContentAlignment="Center"
                     Padding="8,0,8,0"
                     VerticalAlignment="Center"
                     Text="{Binding FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </DockPanel>

        <Border Grid.Row="2"
                BorderThickness="1"
                BorderBrush="#FFD4DEE9"
                Background="White">
            <TreeView x:Name="LibraryTreeView"
                      ItemsSource="{Binding Nodes}"
                      IsEnabled="{Binding IsLibraryEnabled}"
                      ItemContainerStyle="{StaticResource FlowBlox.SubtleTreeViewItemStyle}"
                      BorderThickness="0"
                      Margin="4,5"
                      PreviewMouseMove="LibraryTreeView_PreviewMouseMove">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal"
                                    MinHeight="22">
                            <Grid Width="18"
                                  Height="18"
                                  Margin="0,0,8,0"
                                  VerticalAlignment="Center">
                                <iconPacks:PackIconMaterial Kind="FolderMultipleOutline"
                                                            Width="16"
                                                            Height="16"
                                                            Foreground="#1E88E5"
                                                            VerticalAlignment="Center"
                                                            HorizontalAlignment="Center">
                                    <iconPacks:PackIconMaterial.Style>
                                        <Style TargetType="{x:Type iconPacks:PackIconMaterial}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCategory}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </iconPacks:PackIconMaterial.Style>
                                </iconPacks:PackIconMaterial>
                                <Image Source="{Binding Icon}"
                                       Width="16"
                                       Height="16"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center">
                                    <Image.Style>
                                        <Style TargetType="{x:Type Image}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsFlowBlock}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Image.Style>
                                </Image>
                            </Grid>
                            <TextBlock Text="{Binding DisplayName}"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Border>
    </Grid>
</UserControl>
