<mah:MetroWindow  
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    x:Class="FlowBlox.UICore.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="clr-namespace:FlowBlox.UICore.ViewModels"
    xmlns:resources="clr-namespace:FlowBlox.UICore.Resources"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    Title="{x:Static resources:LoginWindow.LoginWindow_Title}" Height="280" Width="400"
    mc:Ignorable="d">

    <Window.DataContext>
        <viewModel:LoginWindowViewModel />
    </Window.DataContext>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FlowBlox.UICore;component/Views/MahAppMetroResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="{x:Static resources:LoginWindow.Label_Email}" Grid.Row="0" />
        <TextBox Text="{Binding Email}" Grid.Row="1" Margin="0,5,0,10"/>

        <Label Content="{x:Static resources:LoginWindow.Label_Password}" Grid.Row="2" />
        <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Grid.Row="3" Margin="0,5,0,10"/>

        <!-- Checkbox für das Speichern der Benutzerdaten -->
        <CheckBox Content="{x:Static resources:LoginWindow.CheckBox_SaveCredentials}" 
                  IsChecked="{Binding SaveUserCredentials}" 
                  HorizontalAlignment="Right" 
                  Grid.Row="4" 
                  Margin="0,0,0,10"/>

        <!-- Passwort vergessen Link -->
        <TextBlock Grid.Row="5" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Hyperlink Command="{Binding ForgotPasswordCommand}" Foreground="Blue">
                Passwort vergessen?
            </Hyperlink>
        </TextBlock>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="6" Margin="0,10,0,0">
            <Button Command="{Binding CancelCommand}" Content="{x:Static resources:LoginWindow.Button_Back}" Margin="0,0,10,0" IsCancel="True" />
            <Button Command="{Binding LoginCommand}" Content="{x:Static resources:LoginWindow.Button_Login}" IsDefault="True" />
        </StackPanel>
    </Grid>
</mah:MetroWindow>
